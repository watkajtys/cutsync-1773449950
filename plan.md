1. Remove ProductionPolishView: The feedback indicates that `ProductionPolishView.tsx` is an unmaintained God Component hallucination. I have already deleted it from `src/views/` and removed its reference in `src/App.tsx`.
2. Refactor TheaterPlayer God Component: `TheaterPlayer.tsx` needs to be refactored to separate video playback, key bindings, and canvas rendering. 
   - Move keyboard shortcuts into a custom hook `useKeyboardShortcuts.ts`.
   - The timeline and playback logic should be kept cleanly separated. It already looks like `TimelineScrubber` and `PlaybackControls` are extracted, but the `handleTimeUpdate` logic in `TheaterPlayer.tsx` is mixing annotation state (notes matching) with time updates. 
3. Refactor ReviewContext: `ReviewContext.tsx` is doing too much. The annotation state (`shapes`, `currentShape`, `activeTool`, `activeColor`) should probably be separated from the playback state (`currentTime`, `videoRef`, `seekToTime`) and notes fetching logic. However, since they are all part of `useReview()`, splitting `ReviewContext` into `AnnotationContext` and `PlaybackContext` might be too disruptive. Instead, I should see if I can isolate the logic into custom hooks that are then composed in `ReviewProvider`. Wait, the feedback says: "Additionally, 'TheaterPlayer.tsx' and 'ReviewContext.tsx' are becoming monolithic bottlenecks, mixing video playback orchestration, keyboard shortcut handling, and annotation state management." So I must refactor `ReviewContext` to separate these concerns.
   - Wait, if I extract `AnnotationContext` from `ReviewContext`, I would need to update everywhere `useReview` is used. This is doable. Let's see all usages.
4. Linter Check: Run `npm run lint` and verify everything passes without warnings.
5. Playwright Tests: The tests passed after I removed `ProductionPolishView` from `verify.spec.ts`. I should make sure the refactoring doesn't break `verify.spec.ts`.
